import { Component } from '@angular/core';

// Componente raíz de toda la app (el que carga index.html con <app-root>)
@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
})
export class AppComponent {}
